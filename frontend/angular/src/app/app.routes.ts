import { Routes } from '@angular/router';
import { TodolistComponent } from './website/todolist/todolist.component';

export const routes: Routes = [
    {
        component: TodolistComponent,
        path: ""
    }
];